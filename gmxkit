#!/usr/bin/env bash
# GmxKit — global launcher (install once, run from any project folder)
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export GMXKIT_HOME="${ROOT}"
exec bash "${ROOT}/mdprep/md.sh" "$@"

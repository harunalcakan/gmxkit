# Example systems

Optional demo inputs for testing GmxKit. **Not used during install.**

| Folder | Description |
|--------|-------------|
| `metalloenzyme-sample/` | Metalloenzyme test case (CA II + inhibitor). Copy `protein.pdb` and `ligand.mol2` into your project folder. |

For metalloenzyme settings, see `mdprep/profiles/ca2_6i0l_2q38.env` and copy relevant lines into your project's `gmxkit.env`.

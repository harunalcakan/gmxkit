# Python / venv
.venv/
__pycache__/
*.pyc
.cgenff_python_path
.venv_path

# Pipeline runtime (legacy in-tree + per-project .gmxkit/)
mdprep/.state/
mdprep/logs/
mdprep/backups/
.state/
.gmxkit/
gmxkit.env

# GROMACS simulation outputs (regenerated by ./md)
*.xtc
*.trr
*.edr
*.cpt
*.tpr
*.gro
*.log
mdout.mdp

# Generated topology / prep artifacts (keep templates in repo via *.mdp, *.pdb, *.mol2)
topol*.itp
posre*.itp
lig.itp
lig.top
lig.prm
lig_ini.pdb
complex.gro
solv*.gro
processed.gro
# keep source inputs:
!protein.pdb
!protein_prep.pdb
!ligand.mol2
!ligand_fix.mol2
!ligand_sorted.mol2

# Index / analysis scratch
index*.ndx
*.xvg

# Force field is bundled in this repo (charmm36-*.ff at install root).
# Project folders use a symlink; do not commit extra .ff copies under projects.

# Slurm (optional local copies)
*.slurm

# OS / editor
desktop.ini
dist/
.DS_Store
Thumbs.db
topol.top
protein_prep.pdb
*.swp
*~

# Misc local analysis
lig_analysis_output.txt
lig_deep_analysis.txt
lig_resistance_figure_data.csv
=2.8,
